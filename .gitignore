# Dependencies
node_modules/

# Expo / native build output
.expo/
dist/
web-build/
android/
ios/

# Firebase native config (contains project keys — keep out of the repo)
google-services.json
GoogleService-Info.plist

# Env / secrets
.env
.env.local

# Logs & OS
*.log
.DS_Store
npm-debug.*
yarn-error.*

# Local build-debug artifacts
build-raw.bin
build-phase.*
eas-build.log
